{% extends 'base.html.twig' %}

{% block title %}Filiere {% endblock %}

{% block body %}

<div>
                       <a href="{{ path('new_filiere') }}"  class="btn btn-lg btn-info"><i class="material-icons">&#xE147;</i> <span>Ajouter  Filiere</span></a>
                   </div>
  <table class="table table-striped table-hover" class="col-sm-8">
     <thead>

                        <tr>
                            <th>#</th>
                             <th>Libelle</th>
                              <th>Action</th>
                             
                    </thead>
                   <tbody>
                   {% for filiere in filieres %}
                      <tr>
                           
                            <td>{{ filiere.idFiliere }}</td>
                            <td>{{ filiere.libelle}}</td>
                            
                           <td>
                             <a  href="show/{{filiere.idFiliere}}" class="glyphicon glyphicon-cog"><i style="font-size:24px" class="fa" title="Show">&#xf007;</i></a>
                                <a href="edit/{{filiere.idFiliere}}" class="edit" ><i class="material-icons" data-toggle="tooltip" title="Edit">&#xE254;</i></a>
                              <a href="delete/{{filiere.idFiliere}}"class="edit" ><i class="material-icons" data-toggle="tooltip" title="Delete">&#xE872;</i></a> 
                            </td>
                        </tr>


  {% endfor %}
  </tbody>                      
  </table>
    </div>
    </div>

 {% endblock %}